---
description: Audit the Astro project in this directory against baseline best practices.
argument-hint: [url]
---

Audit mode. There is nothing to infer — the user asked for this directly.

`$ARGUMENTS`, if not empty, is the base URL to also audit live (`--url`).

@${CLAUDE_PLUGIN_ROOT}/skills/rider/references/AUDIT.md
